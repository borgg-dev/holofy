fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
httpx==0.27.2
python-multipart==0.0.20   # capture upload: multipart/form-data stills

# pre-grade: in-house centering measurement (numpy core) + capture decode (Pillow)
numpy==2.4.4
pillow==12.2.0

# in-house identification OCR: RapidOCR (PaddleOCR models exported to ONNX). Pure-pip,
# CPU-only, ships its own models, no system Tesseract — the keyless owned recognizer. Only
# loaded when HOLOFY_RECOGNITION_PROVIDER=inhouse; mock/test runs don't import it.
rapidocr-onnxruntime==1.4.4
onnxruntime==1.27.0

# in-house identification: visual recognition. OpenCV does card localization + perspective
# rectification (find the card's quad in a phone photo and flatten it upright); the perceptual
# hash that matches the rectified art against the catalog index is pure numpy (no extra dep).
# Headless build — no GUI/X11 libs — is the right one for the inference container.
opencv-python-headless==4.13.0.92

# persistence
sqlalchemy[asyncio]==2.0.36
alembic==1.14.0
asyncpg==0.30.0       # prod: Postgres (EU region)
aiosqlite==0.20.0     # tests + keyless local runs

# dev / test
pytest==8.3.4
pytest-asyncio==0.25.0
redis==5.3.1
fakeredis[lua]==2.26.2   # Redis limiter tests: EVALSHA needs the Lua (lupa) extra
async-timeout==5.0.1     # redis dep, required on Python < 3.11.3
