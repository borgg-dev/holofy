node_modules/
.expo/
dist/
web-build/
*.log
.DS_Store

# Native build outputs (Expo prebuild / EAS)
/ios
/android

# The OFL fonts (Clash Display, Manrope) ARE committed — the app statically requires
# them and the Metro bundle fails without them. OFL permits redistribution.
dist-web/
